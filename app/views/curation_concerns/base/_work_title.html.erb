
<% presenter.title.each_with_index do |title, index| %>
    <% if index == 0 %>
        <h1><%= title %> <%= presenter.permission_badge %></h1>
    <% else %>
        <h1><%= title %></h1>
    <% end %>
<% end %>
<!-- Disable until Unit show page finished -->
<!--<span class="unit-title">- from the <a href="#"><%= Unit.find_by_key(presenter.unit).name %></a></span>-->
